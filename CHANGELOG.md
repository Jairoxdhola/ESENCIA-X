# Changelog

## v2.0 - 2026-07-25
- Added Infinite Yield support (`load_iy`, `execute_iy`)
- Added Drawing tools (`drawing_create`, `drawing_set`, `drawing_fonts`, `drawing_clear`)
- Auto-updater with `UPDATE.bat` and `update.py`
- Automatic installer with `INSTALL.bat` and `install.py`
- ESENCIA X banner in the bridge console
- Improved error handling in `server.py`
- Added `.gitignore`
- `LICENSE` MIT

## v1.0 - 2026-07-24
- Initial release
- MCP server with 20 tools
- WebSocket bridge for Roblox via Potassium
- Tools: `execute_lua`, `get_instances`, `get_scripts`, `get_gc`, `fire_signal`, `hook_function`, `get_environment`, `simulate_input`, `read_file`, `write_file`, `list_files`, `http_request`, `decompile_script`, `save_instance`, `set_clipboard`, `get_player_info`, `teleport_to`
